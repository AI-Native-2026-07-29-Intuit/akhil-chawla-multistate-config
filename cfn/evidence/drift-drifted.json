# Drift detection — not run (Floci does not support DetectStackDrift).
# On real AWS, after an out-of-band tag on the artefacts bucket,
# describe-stack-drift-detection-status would report StackDriftStatus: DRIFTED
# and MultistateArtifactsBucket would appear as MODIFIED in resource drifts.
